# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.log

# Editor folders
.vscode/
.idea/

# Node (not used by the site, but just in case)
node_modules/
dist-cms/
.wrangler/

# Temporary image staging / candidate photos
images/_cand/

# Oversized raw source video (exceeds GitHub 100MB limit); web version is virtual-tour.mp4
videos/1080p.mov
